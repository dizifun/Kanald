name: Kanal D Full Arşiv Güncelleyici

on:
  schedule:
    # Her gün sabah 06:00 ve 18:00'de çalışır
    - cron: '0 6,18 * * *'
  workflow_dispatch: # Manuel çalıştırma butonu

permissions:
  contents: write

jobs:
  scrape_and_archive:
    runs-on: ubuntu-latest

    steps:
      # 1. Repoyu Çek (Derinlik 0: Tüm geçmişi çek ki hata vermesin)
      - name: 📥 Repoyu Çek
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # 2. Python Kur
      - name: 🐍 Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # 3. Kütüphaneleri Yükle
      - name: 📦 Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 tqdm

      # 4. Klasör Yapısını Garantiye Al
      - name: 📂 Klasör Kontrolü
        run: mkdir -p KanalD_Arsiv

      # 5. Scripti Çalıştır
      - name: 🚀 Kanal D Tarayıcıyı Çalıştır
        run: |
          # Eğer dosya adın farklıysa burayı düzelt:
          python kanald_full_scraper.py

      # 6. Kontrol Et (Loglarda dosya var mı bakmak için)
      - name: 🔍 Dosyaları Listele
        run: ls -R KanalD_Arsiv/

      # 7. SENKRONİZASYON VE KAYIT (En Önemli Kısım)
      - name: 💾 Kaydet ve Yükle
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # Tüm yeni dosyaları ekle
          git add KanalD_Arsiv
          
          # Tarih bilgisini al (Commit mesajı için)
          TIMESTAMP=$(date +'%Y-%m-%d %H:%M')
          
          # Değişiklik varsa commit at, yoksa çık
          if git diff --staged --quiet; then
            echo "✅ Değişiklik yok. Repo güncel."
          else
            git commit -m "Kanal D Arşiv Güncellemesi: $TIMESTAMP"
            
            # --- KRİTİK NOKTA ---
            # GitHub'daki değişiklikleri çek ve bizimkilerle birleştir
            git pull --rebase origin main
            
            # Şimdi yükle
            git push origin main
          fi
